name: Publish npm

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Existing git tag to publish, for example v0.0.1"
        required: true
        type: string
      npm_tag:
        description: "npm dist-tag to publish"
        required: true
        default: next
        type: choice
        options:
          - next
          - latest
          - alpha
          - beta

permissions:
  contents: read
  id-token: write

concurrency:
  group: publish-npm
  cancel-in-progress: false

jobs:
  publish:
    name: Publish @o3dotdev/code
    runs-on: ubuntu-latest
    environment: npm-publish
    env:
      PACKAGE_DIR: packages/code
      PACKAGE_NAME: "@o3dotdev/code"
    steps:
      - name: Resolve release inputs
        id: release
        shell: bash
        run: |
          if [[ "${GITHUB_EVENT_NAME}" == "release" ]]; then
            tag_name="$(node -p "require(process.env.GITHUB_EVENT_PATH).release.tag_name")"
            is_prerelease="$(node -p "require(process.env.GITHUB_EVENT_PATH).release.prerelease")"
            if [[ "${is_prerelease}" == "true" ]]; then
              npm_tag="next"
            else
              npm_tag="latest"
            fi
          else
            tag_name="${{ inputs.release_tag }}"
            npm_tag="${{ inputs.npm_tag }}"
          fi

          if [[ "${tag_name}" != v* || "${tag_name}" == "v" ]]; then
            echo "Release tag must be v<semver>; received '${tag_name}'." >&2
            exit 1
          fi

          release_version="${tag_name#v}"

          {
            echo "tag_name=${tag_name}"
            echo "release_version=${release_version}"
            echo "npm_tag=${npm_tag}"
          } >> "${GITHUB_OUTPUT}"

      - name: Checkout release tag
        uses: actions/checkout@v6
        with:
          ref: ${{ steps.release.outputs.tag_name }}

      - name: Set up pnpm
        uses: pnpm/action-setup@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          package-manager-cache: false

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Set package release version
        shell: bash
        working-directory: packages/code
        run: |
          npm version "${{ steps.release.outputs.release_version }}" --no-git-tag-version --workspaces=false

      - name: Validate release version
        shell: bash
        run: |
          package_version="$(node -p "require('./${PACKAGE_DIR}/package.json').version")"

          if [[ "${package_version}" != "${{ steps.release.outputs.release_version }}" ]]; then
            echo "Release tag must be v<semver> and will publish that version; package rewrite produced ${package_version}." >&2
            exit 1
          fi

          if npm view "${PACKAGE_NAME}@${{ steps.release.outputs.release_version }}" version >/dev/null 2>&1; then
            echo "${PACKAGE_NAME}@${{ steps.release.outputs.release_version }} is already published." >&2
            exit 1
          fi

      - name: Check formatting
        run: pnpm format:check

      - name: Check runtime payload boundaries
        run: pnpm native-binaries:check

      - name: Test package
        run: pnpm --filter @o3dotdev/code test

      - name: Smoke package launcher
        run: pnpm --filter @o3dotdev/code smoke

      - name: Publish to npm
        working-directory: packages/code
        run: npm publish --access public --tag "${{ steps.release.outputs.npm_tag }}"
